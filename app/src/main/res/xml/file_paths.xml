<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Carpeta de caché para fotos temporales de la cámara -->
    <cache-path name="camera_images" path="." />

    <!-- Carpeta de archivos internos si se necesita en el futuro -->
    <files-path name="internal_files" path="." />
</paths>
